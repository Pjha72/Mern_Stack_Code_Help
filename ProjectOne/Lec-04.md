# HTML : Hyper Text Markup language
# What is HTML?
```define Structure```

# Doctype : Define the document in HTML5
# tag : it is special words that can provide the particular utility
# element : Combination of opening tag and content and closing tag
# html tag : opening tag => <html> closing tag => </html> , it is a root element
# head tag : opening tag => <head> closing tag => </head>, metadata/seo/title
# body tag : define the labeling or content
# comment : ctrl+/

# resources : MDN web docs